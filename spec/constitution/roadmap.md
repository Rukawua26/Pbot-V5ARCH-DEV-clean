# Roadmap

Source of truth for pending improvements: `docs/roadmap/mejoras-pendientes.md`.

## Pending Improvements

- FVG Tracker statistical measurement in `PAPER` or `SHADOW`.
- Global Market Provider validation in `PAPER` or `SHADOW`.
- Macro-reactive filters threshold validation and additional macro rules.
- Future auto-replication of winning strategies after enough SHADOW data exists.
- Dashboard API key configuration or explicit disable flow.
- Direction-by-agent-consensus and adaptive trailing validation with closed SHADOW trades.

## Success Bias

- Evidence before execution integration.
- SHADOW validation before REAL impact.
- Runtime safety gates remain active.
- Coverage gate stays at or above 75% unless a spec explicitly justifies a change.
